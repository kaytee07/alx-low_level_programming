#include "main.h"
#include <stdio.h>


/**
 *
 *
 *
 *
 */

void _puts(char *str)
{
puts(str);
}


int main(void)
{
    char *str;

    str = "I do not fear computers. I fear the lack of them - Isaac Asimov";
    _puts(str);
    return (0);
}
