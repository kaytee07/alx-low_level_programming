#include <stdio.h>
#include "main.h"

char *_strcat(char *dest, char *src)
{
int i = 0, j = 0;
while(src[i] != '\0')
{
if(dest[j] == '\0' || !dest[j])
{
dest[j] = src[i];
i++;
}
j++;
}
dest[j] = '\0';
return (dest);
}

