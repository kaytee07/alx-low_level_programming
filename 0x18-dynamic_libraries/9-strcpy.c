#include "main.h"
#include <stdio.h>
#include <string.h>

/**
 * _strcpy - copy string from src to dest pointer including null optor
 * @dest: first parameter
 * @src: second parameter
 * Return: return pointer to dest
 */

char *_strcpy(char *dest, char *src)
{
strcpy(dest, src);
return (dest);
}
