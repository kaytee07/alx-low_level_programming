#include "main.h"
#include <stdio.h>
#include <string.h>


/**
 * _strlen - print the lenght of the string
 * @s: first argument which is a pointer
 * Return: the lenth of the argument passed
 */
int _strlen(char *s)
{
return (strlen(s));
}

