#include <stdio.h>

/**
 *main - print string followed by a newline using printf
 *Return: return 0 if function runs succesfully
 */
int main(void)
{
printf("with proper grammar, but the outcome is a piece of art,\n");
 return (0);
}
