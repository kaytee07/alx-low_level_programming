#include <stdio.h>
#include <time.h>
#include "main.h"

void jack_bauer(void)
{
time_t seconds;
struct tm *timestruct;
seconds = time(NULL);
printf("%d:%d", timeStruct->tm_hour, timeStruct->tm_min);
}
